spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:batch-test-db
    driver-class-name: org.h2.Driver
    username: sa
    password: 1234

batch:
  input:
    folder: files/input
  output:
    folder: files/output

# SFTP 테스트 환경에서는 비활성화
sftp:
  enabled: false
